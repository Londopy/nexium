---
name: Bug report
about: The compiler crashed, accepted a wrong program, rejected a right one, or generated code that misbehaves
labels: bug
---

**Compiler version** (`nx version`) and platform:

**Minimal program** (the smallest `.nx` file that shows the problem):

```
fn main() {
}
```

**Command** you ran (`nx run`, `nx ship`, ...):

**What happened** (paste the diagnostic or the output; `nx emit-c` output helps for wrong code):

**What you expected**, with the section of `nexium-spec.txt` or `nexium-systems-spec.txt` it follows from, if any:
