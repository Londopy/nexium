// EXPECT: this returns a slice into `v`, which views a temporary
// `for (k, v) in m` walks copies of the entries, freed after the loop
fn value_of(m: *Map(String, String), key: []u8) -> []u8 {
    for (k, v) in m.* {
        if k == key { return v[..] }
    }
    return ""
}

fn main() { }
